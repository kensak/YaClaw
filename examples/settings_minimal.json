{
    "channel": {
        "discord": {
            "plugin": "channel_discord",
            "channel_id": "${DISCORD_CHANNEL_ID}",
            "bot_token": "${DISCORD_BOT_TOKEN}",
            "agent": "copilot",
            "require_mention": true
        }
    },
    "agent": {
        "copilot": {
            "plugin": "agent_copilot_cli",
            "work_dir": "workspace/copilot",
            "default_channel": "discord"
        }
    }
}
